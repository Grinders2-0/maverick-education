import React, { memo } from "react";

const DashBoard = () => {
  return (
    <div style={{}} className="">
      <div>
        <h1>hello style</h1>
      </div>
    </div>
  );
};

export default memo(DashBoard);
